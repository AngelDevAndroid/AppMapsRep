<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppMaps" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AppMaps" parent="Base.Theme.AppMaps" />

    <style name="ToolbarTitleText" parent="Widget.AppCompat.Toolbar">
        <item name="android:textSize">14sp</item> <!-- Cambia el tamaño -->
        <!--<item name="android:textColor">#FFFFFF</item>  (Opcional) Cambia color -->
    </style>
</resources>